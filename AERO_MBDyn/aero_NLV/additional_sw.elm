joint: CURR_BLADE + PITCH_LINK, distance,
	CURR_BLADE,
		position, reference, CURR_BLADE + PITCH_LINK_TOP, null,
	SWASHPLATE_ROTATING,
		position, reference, CURR_BLADE + PITCH_LINK_BOTTOM, null,
	from nodes; # must be |Z_SWASHPLATE|
	# -Z_SWASHPLATE;

# vim:ft=mbd

